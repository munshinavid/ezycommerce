# Development override — use with: docker compose -f docker-compose.yml -f docker-compose.dev.yml up
services:
  app:
    volumes:
      - ./:/var/www/html
    environment:
      APP_ENV: development
      APP_DEBUG: "true"
